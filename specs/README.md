# Specs

All specs for AI to generate code.

## Index

- [Architecture Design](architecture-design.md) - Comprehensive architecture spec: workspace layout, crate boundaries, module structure, key trait definitions, dependency list (latest versions), error handling strategy, command registration pattern (clap), authentication flow, HTTP client architecture, output formatting, configuration system, git integration, extension system, and Go-to-Rust module mapping table
